{
  "name": "chatorama",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/chatalog/*"
  ],
  "scripts": {
    "build": "npm -ws run build",
    "dev": "npm -ws run dev",
    "test": "npm -ws run test"
  },
  "engines": { "node": ">=20" }
}
