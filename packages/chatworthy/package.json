{
  "name": "chatworthy",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run clean && tsx scripts/build.ts",
    "watch": "tsx scripts/build.ts --watch",
    "clean": "rm -rf dist && mkdir -p dist"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0"
  },
  "dependencies": {
    "@chatorama/chat-md-core": "file:../../chat-md-core"
  }
}
