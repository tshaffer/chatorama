import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    mode: 'light',
    primary: { main: '#2f6feb' },   // calm blue
    secondary: { main: '#00a67d' }  // teal accent
  }
});
